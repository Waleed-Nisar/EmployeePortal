# EmployeePortal

A simple **Employee Management Portal** built with **ASP.NET MVC** using .NET and Razor views.  
This web application implements basic CRUD (Create, Read, Update, Delete) operations for employee records using the **Model-View-Controller (MVC) architecture**, giving you a clean separation between UI, business logic, and data.:contentReference[oaicite:0]{index=0}

---

##  Features

✔ MVC architecture for clean, scalable code  
✔ Add, edit, view, and delete employee details  
✔ Uses Razor views for UI  
✔ Server-side validation for data integrity  
✔ SQL Server database support via Entity Framework

---

##  Architecture

This project follows the **Model-View-Controller (MVC)** pattern:

- **Models** — represent the data structure (Employee, Department, etc.)  
- **Views** — UI templates rendered by Razor  
- **Controllers** — handle HTTP requests and coordinate between Views and Models

MVC helps separate concerns and improves maintainability, testability, and scalability in web apps.:contentReference[oaicite:1]{index=1}

---

## 🛠️ Built With

This project uses:

- **ASP.NET MVC (C#)**  
- **Entity Framework** for data access  
- **SQL Server (LocalDB or full SQL Server)**  
- **Razor Views (HTML/CSS)**

---

